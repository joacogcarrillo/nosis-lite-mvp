{
  "name": "nosis-lite-mvp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "test": "node tests/worker.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.24.0"
  }
}
